DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,testserver
# Local Postgres (optional). Leave unset to use SQLite via manage.py runserver.
DATABASE_URL=postgres://notesy:notesy_dev_pw@localhost:5432/notesy
SUMMARIZER_API_KEY=
SUMMARIZER_URL=https://api.summarize.example.com/v1

# Docker: copy this file to .env only if you want to override compose defaults, then:
#   docker compose up --build
# App: http://localhost:8000  login demo / demo
